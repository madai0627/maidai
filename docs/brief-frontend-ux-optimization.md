# 前端用户体验优化方案

> 📅 创建日期: 2024-12-17  
> 📊 版本: v1.0  
> 👤 作者: Mary (Business Analyst)  
> 🎯 状态: 待评审

---

## 1. 执行摘要 (Executive Summary)

**项目名称：** 前端用户体验优化

**优化目标：**
针对 maidai 个人管理系统的前端进行全面的用户体验优化，解决当前存在的首页定位偏差、导航入口混乱、信息架构不清晰等问题，提升用户使用效率和满意度。

**核心问题：**
1. 首页被"学习模块"独占，与"个人管理系统"定位严重脱节
2. 管理后台入口隐藏，可发现性为零
3. 模块命名与实际功能不符（如"宠物"实为"照片墙"）
4. 路由命名不规范，用户体验和 SEO 不友好
5. 快速入口偏科，其他模块缺少快捷操作

**预期收益：**
- 提升用户对系统功能的认知完整度
- 减少用户操作路径，提高使用效率
- 增强系统的专业感和一致性

---

## 2. 现状分析 (Current State Analysis)

### 2.1 首页问题

#### 当前首页结构

```
┌─────────────────────────────────────────────────────────────┐
│  [导航栏]                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 快速开始                                              │   │
│  │ [开始做题] [复习错题] [我的收藏]    ← 只有学习相关      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  📚 学习仪表板    ← 标题暴露定位问题                         │
│                                                             │
│  [总做题数] [总得分] [正确率] [收藏题目]  ← 只有学习数据      │
│                                                             │
│  [分类答题分布图]  [难度分布图]          ← 只有学习图表      │
│                                                             │
│  [最近7天学习趋势]                       ← 只有学习趋势      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 问题诊断

| 问题 | 严重程度 | 影响 |
|------|---------|------|
| 首页标题"学习仪表板"与系统定位不符 | 🔴 严重 | 用户困惑，误以为是学习专用系统 |
| 快速入口只有做题相关 | 🔴 严重 | 日记/财务/照片用户需要额外点击 |
| 统计卡片只展示学习数据 | 🟡 中等 | 其他模块缺乏存在感 |
| 图表只展示学习数据 | 🟡 中等 | 首页价值对非学习用户为零 |

### 2.2 导航问题

#### 当前导航结构

```
Navbar: [首页] [日记] [宠物] [财务] [做题]    ⚙️ [用户名▼]
                  │                          │
                  │                          └─→ 只跳转到题库管理
                  │                              其他管理入口被隐藏
                  │
                  └─→ 实际功能是"照片墙"，命名误导
```

#### 问题诊断

| 问题 | 严重程度 | 影响 |
|------|---------|------|
| 设置按钮只到题库管理 | 🔴 严重 | 其他管理功能可发现性为零 |
| "宠物"导航实际是照片墙 | 🟡 中等 | 用户期望与实际不符 |
| 管理后台入口分散 | 🟡 中等 | 需要先进入某管理才能发现其他 |

### 2.3 路由问题

#### 当前路由结构

```javascript
// 用户功能路由（命名不规范）
/                      → 首页
/index-quiz            → 做题
/index-quiz-wrong      → 错题
/index-quiz-favorites  → 收藏
/index-cat             → 宠物（实为照片墙）
/index-finance         → 财务
/index-diary           → 日记

// 管理后台路由（命名不统一）
/quiz-admin/*          → 题库管理
/cat/*                 → 宠物管理
/finance/*             → 财务管理
/system/*              → 系统管理
```

#### 问题诊断

| 问题 | 严重程度 | 影响 |
|------|---------|------|
| `/index-xxx` 命名不直观 | 🟡 中等 | URL 不可读，SEO 不友好 |
| 管理路由前缀不统一 | 🟡 中等 | 代码维护困难 |
| 功能路由与管理路由混淆 | 🟡 中等 | 权限控制困难 |

### 2.4 信息架构问题

```
当前架构（混乱）：

maidai
├── / (首页=学习仪表板)           ← 定位错误
├── /index-xxx (用户功能)         ← 命名不规范
│   ├── quiz, quiz-wrong, quiz-favorites
│   ├── cat (实为照片墙)          ← 命名误导
│   ├── finance
│   └── diary
├── /xxx-admin 或 /xxx (管理)     ← 不统一
│   ├── quiz-admin/*
│   ├── cat/*
│   ├── finance/*
│   └── system/*
└── /login
```

---

## 3. 优化方案 (Optimization Plan)

### 3.1 首页重新设计

#### 3.1.1 新首页定位

**从"学习仪表板"变为"个人中心"**

设计原则：
- 所有模块平等展示
- 提供各模块快捷入口
- 汇总展示用户活动数据
- 时间线展示最近动态

#### 3.1.2 新首页布局

```
┌─────────────────────────────────────────────────────────────┐
│  [导航栏]  [个人中心] [日记] [照片墙] [财务] [学习]  [管理▼] [用户▼] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  👋 你好，[用户名]！今天是 2024年12月17日 星期二              │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                    快速操作                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ 📔 日记    │ │ 🖼️ 照片墙  │ │ 💰 财务    │ │ 📚 学习    │   │
│  │           │ │           │ │           │ │           │   │
│  │ 今日情绪   │ │ 共56张    │ │ 本月支出   │ │ 今日做题   │   │
│  │ 未记录    │ │ 照片      │ │ ¥2,345    │ │ 15 道     │   │
│  │           │ │           │ │           │ │           │   │
│  │ [写日记]   │ │ [上传照片] │ │ [记一笔]   │ │ [继续学习] │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📊 本周概览                                          │   │
│  │                                                     │   │
│  │  日记      照片墙      财务         学习              │   │
│  │  ━━━━      ━━━━       ━━━━         ━━━━              │   │
│  │  5 篇      +3 张      12 笔        30 题             │   │
│  │  连续3天   本周新增    支出¥856     正确率 85%        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📅 最近动态                                          │   │
│  │                                                     │   │
│  │  12-17 15:30  📔 写了日记「今天很开心」        [查看]  │   │
│  │  12-17 12:20  💰 记账 -88元 [午餐]            [详情]  │   │
│  │  12-17 10:15  🖼️ 上传了3张照片                [查看]  │   │
│  │  12-17 09:00  📚 完成10道题，正确率90%         [继续]  │   │
│  │  12-16 22:30  📔 写了日记「周末计划」          [查看]  │   │
│  │                                                     │   │
│  │                    [查看更多动态]                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 3.1.3 首页组件拆分

```
src/views/home/
├── Index.vue                    # 首页主容器
├── components/
│   ├── WelcomeHeader.vue        # 欢迎语 + 日期
│   ├── QuickActions.vue         # 快速操作卡片组
│   ├── WeeklyOverview.vue       # 本周概览统计
│   └── RecentActivities.vue     # 最近动态时间线
```

### 3.2 导航系统优化

#### 3.2.1 顶部导航条重设计

**优化前：**
```
[首页] [日记] [宠物] [财务] [做题]    ⚙️ [用户名▼]
```

**优化后：**
```
[🏠 个人中心] [📔 日记] [🖼️ 照片墙] [💰 财务] [📚 学习]    [⚙️ 管理▼] [👤 用户名▼]
```

#### 3.2.2 管理入口下拉菜单

```vue
<!-- 管理下拉菜单 -->
<el-dropdown>
  <span class="admin-trigger">
    <el-icon><Setting /></el-icon>
    管理
    <el-icon><ArrowDown /></el-icon>
  </span>
  <template #dropdown>
    <el-dropdown-menu>
      <el-dropdown-item @click="$router.push('/admin/quiz')">
        <el-icon><Collection /></el-icon>
        题库管理
      </el-dropdown-item>
      <el-dropdown-item @click="$router.push('/admin/photos')">
        <el-icon><Picture /></el-icon>
        照片管理
      </el-dropdown-item>
      <el-dropdown-item @click="$router.push('/admin/finance')">
        <el-icon><Coin /></el-icon>
        财务管理
      </el-dropdown-item>
      <el-dropdown-item divided @click="$router.push('/admin/system')">
        <el-icon><Setting /></el-icon>
        系统管理
      </el-dropdown-item>
    </el-dropdown-menu>
  </template>
</el-dropdown>
```

#### 3.2.3 导航激活状态优化

使用路由 `meta` 字段统一管理激活状态：

```javascript
// 路由配置
{
  path: '/diary',
  meta: { navKey: 'diary' }  // 用于导航高亮
}

// 导航组件
const isActive = (navKey) => route.meta?.navKey === navKey
```

### 3.3 路由结构优化

#### 3.3.1 新路由命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 用户功能 | `/{module}` | `/diary`, `/finance` |
| 用户子功能 | `/{module}/{action}` | `/study/wrong` |
| 管理后台 | `/admin/{module}` | `/admin/quiz` |
| 管理子功能 | `/admin/{module}/{action}` | `/admin/quiz/categories` |

#### 3.3.2 完整路由表

```javascript
// src/router/index.js

const routes = [
  // ============ 认证相关 ============
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/auth/Login.vue'),
    meta: { requiresAuth: false, layout: 'blank' }
  },

  // ============ 用户功能区 ============
  {
    path: '/',
    component: () => import('@/layouts/AppLayout.vue'),
    children: [
      // 个人中心（首页）
      {
        path: '',
        name: 'Home',
        component: () => import('@/views/home/Index.vue'),
        meta: { title: '个人中心', navKey: 'home' }
      },
      
      // 日记模块
      {
        path: 'diary',
        name: 'Diary',
        component: () => import('@/views/diary/Index.vue'),
        meta: { title: '日记', navKey: 'diary' }
      },
      
      // 照片墙模块
      {
        path: 'photos',
        name: 'Photos',
        component: () => import('@/views/photos/Index.vue'),
        meta: { title: '照片墙', navKey: 'photos' }
      },
      
      // 财务模块
      {
        path: 'finance',
        name: 'Finance',
        component: () => import('@/views/finance/Index.vue'),
        meta: { title: '财务', navKey: 'finance' }
      },
      
      // 学习模块
      {
        path: 'study',
        name: 'Study',
        component: () => import('@/views/study/Index.vue'),
        meta: { title: '学习', navKey: 'study' }
      },
      {
        path: 'study/wrong',
        name: 'StudyWrong',
        component: () => import('@/views/study/Wrong.vue'),
        meta: { title: '错题本', navKey: 'study' }
      },
      {
        path: 'study/favorites',
        name: 'StudyFavorites',
        component: () => import('@/views/study/Favorites.vue'),
        meta: { title: '我的收藏', navKey: 'study' }
      }
    ]
  },

  // ============ 管理后台 ============
  {
    path: '/admin',
    component: () => import('@/layouts/AdminLayout.vue'),
    redirect: '/admin/quiz/categories',
    meta: { requiresAdmin: true },
    children: [
      // 题库管理
      {
        path: 'quiz',
        redirect: '/admin/quiz/categories',
        meta: { title: '题库管理', icon: 'Collection' },
        children: [
          {
            path: 'categories',
            name: 'AdminQuizCategories',
            component: () => import('@/views/admin/quiz/Categories.vue'),
            meta: { title: '分类管理', icon: 'Folder' }
          },
          {
            path: 'questions',
            name: 'AdminQuizQuestions',
            component: () => import('@/views/admin/quiz/Questions.vue'),
            meta: { title: '题目管理', icon: 'Document' }
          }
        ]
      },
      
      // 照片管理
      {
        path: 'photos',
        redirect: '/admin/photos/types',
        meta: { title: '照片管理', icon: 'Picture' },
        children: [
          {
            path: 'types',
            name: 'AdminPhotosTypes',
            component: () => import('@/views/admin/photos/Types.vue'),
            meta: { title: '分类管理', icon: 'FolderChecked' }
          },
          {
            path: 'list',
            name: 'AdminPhotosList',
            component: () => import('@/views/admin/photos/List.vue'),
            meta: { title: '照片管理', icon: 'Picture' }
          }
        ]
      },
      
      // 财务管理
      {
        path: 'finance',
        redirect: '/admin/finance/purpose',
        meta: { title: '财务管理', icon: 'Coin' },
        children: [
          {
            path: 'purpose',
            name: 'AdminFinancePurpose',
            component: () => import('@/views/admin/finance/Purpose.vue'),
            meta: { title: '用途管理', icon: 'Coin' }
          },
          {
            path: 'budget',
            name: 'AdminFinanceBudget',
            component: () => import('@/views/admin/finance/Budget.vue'),
            meta: { title: '预算管理', icon: 'Tickets' }
          },
          {
            path: 'records',
            name: 'AdminFinanceRecords',
            component: () => import('@/views/admin/finance/Records.vue'),
            meta: { title: '记录管理', icon: 'Document' }
          }
        ]
      },
      
      // 系统管理
      {
        path: 'system',
        redirect: '/admin/system/users',
        meta: { title: '系统管理', icon: 'Setting' },
        children: [
          {
            path: 'users',
            name: 'AdminSystemUsers',
            component: () => import('@/views/admin/system/Users.vue'),
            meta: { title: '用户管理', icon: 'User' }
          },
          {
            path: 'roles',
            name: 'AdminSystemRoles',
            component: () => import('@/views/admin/system/Roles.vue'),
            meta: { title: '角色管理', icon: 'UserFilled' }
          }
        ]
      }
    ]
  },

  // ============ 兼容旧路由（重定向）============
  { path: '/index-diary', redirect: '/diary' },
  { path: '/index-cat', redirect: '/photos' },
  { path: '/index-finance', redirect: '/finance' },
  { path: '/index-quiz', redirect: '/study' },
  { path: '/index-quiz-wrong', redirect: '/study/wrong' },
  { path: '/index-quiz-favorites', redirect: '/study/favorites' },
  { path: '/quiz-admin', redirect: '/admin/quiz' },
  { path: '/quiz-admin/:path(.*)', redirect: to => `/admin/quiz/${to.params.path}` },
  { path: '/cat', redirect: '/admin/photos' },
  { path: '/cat/:path(.*)', redirect: to => `/admin/photos/${to.params.path}` },

  // 404
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: () => import('@/views/error/404.vue')
  }
]
```

### 3.4 目录结构优化

#### 3.4.1 新目录结构

```
src/
├── layouts/                        # 布局组件
│   ├── AppLayout.vue              # 用户功能布局（顶部导航）
│   ├── AdminLayout.vue            # 管理后台布局（侧边栏）
│   ├── BlankLayout.vue            # 空白布局（登录页）
│   └── components/
│       ├── AppNavbar.vue          # 用户端导航条
│       ├── AdminSidebar.vue       # 管理端侧边栏
│       ├── AdminHeader.vue        # 管理端顶栏
│       ├── UserDropdown.vue       # 用户下拉菜单
│       └── AdminDropdown.vue      # 管理入口下拉菜单
│
├── views/
│   ├── home/                      # 个人中心（首页）
│   │   ├── Index.vue
│   │   └── components/
│   │       ├── WelcomeHeader.vue
│   │       ├── QuickActions.vue
│   │       ├── WeeklyOverview.vue
│   │       └── RecentActivities.vue
│   │
│   ├── diary/                     # 日记模块
│   │   ├── Index.vue
│   │   └── components/
│   │       ├── DiaryList.vue
│   │       ├── DiaryEdit.vue
│   │       ├── DiaryDetail.vue
│   │       └── DiaryStats.vue
│   │
│   ├── photos/                    # 照片墙模块（原 index-cat）
│   │   └── Index.vue
│   │
│   ├── finance/                   # 财务模块（原 index-finance）
│   │   └── Index.vue
│   │
│   ├── study/                     # 学习模块（原 index-quiz）
│   │   ├── Index.vue
│   │   ├── Wrong.vue
│   │   └── Favorites.vue
│   │
│   ├── admin/                     # 管理后台
│   │   ├── quiz/                  # 题库管理
│   │   │   ├── Categories.vue
│   │   │   └── Questions.vue
│   │   ├── photos/                # 照片管理（原 cat）
│   │   │   ├── Types.vue
│   │   │   └── List.vue
│   │   ├── finance/               # 财务管理
│   │   │   ├── Purpose.vue
│   │   │   ├── Budget.vue
│   │   │   └── Records.vue
│   │   └── system/                # 系统管理
│   │       ├── Users.vue
│   │       └── Roles.vue
│   │
│   ├── auth/                      # 认证相关
│   │   └── Login.vue
│   │
│   └── error/                     # 错误页面
│       └── 404.vue
│
├── components/                    # 通用组件
│   └── diary/
│       ├── MoodSelector.vue
│       ├── MoodChart.vue
│       └── DiaryCard.vue
│
├── router/
│   ├── index.js                   # 主路由文件
│   ├── app.routes.js              # 用户功能路由
│   └── admin.routes.js            # 管理后台路由
│
└── constants/
    ├── diary.js
    └── nav.js                     # 导航配置
```

### 3.5 信息架构优化

#### 3.5.1 新信息架构

```
maidai 个人管理系统
│
├── 用户功能区（顶部 Tab 导航）
│   │
│   ├── 🏠 个人中心 (/)
│   │   ├── 欢迎语 + 日期
│   │   ├── 快速操作（4个模块入口）
│   │   ├── 本周概览统计
│   │   └── 最近动态时间线
│   │
│   ├── 📔 日记 (/diary)
│   │   ├── 日记列表（筛选/搜索）
│   │   ├── 写日记弹窗
│   │   ├── 日记详情弹窗
│   │   └── 情绪统计面板
│   │
│   ├── 🖼️ 照片墙 (/photos)
│   │   ├── 瀑布流照片展示
│   │   ├── 上传照片弹窗
│   │   └── 照片预览
│   │
│   ├── 💰 财务 (/finance)
│   │   ├── 快速记账
│   │   ├── 预算进度
│   │   ├── 统计图表
│   │   └── 历史记录弹窗
│   │
│   └── 📚 学习 (/study)
│       ├── 做题页面
│       ├── 错题本 (/study/wrong)
│       └── 我的收藏 (/study/favorites)
│
└── 管理后台（下拉菜单 + 侧边栏导航）
    │
    ├── 📚 题库管理 (/admin/quiz)
    │   ├── 分类管理
    │   └── 题目管理
    │
    ├── 🖼️ 照片管理 (/admin/photos)
    │   ├── 分类管理
    │   └── 照片管理
    │
    ├── 💰 财务管理 (/admin/finance)
    │   ├── 用途管理
    │   ├── 预算管理
    │   └── 记录管理
    │
    └── ⚙️ 系统管理 (/admin/system)
        ├── 用户管理
        └── 角色管理
```

---

## 4. 实施计划 (Implementation Plan)

### 4.1 分阶段实施

```
Phase 1 (快速修复)        Phase 2 (路由重构)        Phase 3 (首页重设计)
━━━━━━━━━━━━━━━━━        ━━━━━━━━━━━━━━━━        ━━━━━━━━━━━━━━━━━
   1-2 天                    2-3 天                    3-4 天

• 首页添加快速入口         • 创建 Layout 系统        • 设计新首页
• 管理入口下拉菜单         • 路由重命名              • 实现本周概览
• 导航"宠物"改"照片墙"     • 目录结构调整            • 实现最近动态
                          • 旧路由重定向            • 数据聚合 API
```

### 4.2 Phase 1: 快速修复（1-2天）

#### 任务清单

| # | 任务 | 文件 | 工作量 |
|---|------|------|--------|
| 1.1 | 首页添加各模块快捷入口 | `views/Index.vue` | 0.5h |
| 1.2 | 管理入口改为下拉菜单 | `components/Navbar.vue` | 1h |
| 1.3 | 导航"宠物"改名"照片墙" | `components/Navbar.vue` | 5min |
| 1.4 | 修复导航激活状态逻辑 | `components/Navbar.vue` | 0.5h |

#### 1.1 首页添加快速入口

```vue
<!-- views/Index.vue - 快速操作区修改 -->
<div class="action-buttons">
  <!-- 学习 -->
  <el-button type="primary" size="large" @click="$router.push('/index-quiz')">
    <el-icon><Document /></el-icon>
    开始做题
  </el-button>
  <el-button type="success" size="large" @click="$router.push('/index-quiz-wrong')">
    <el-icon><Warning /></el-icon>
    复习错题
  </el-button>
  
  <!-- 新增：日记 -->
  <el-button type="info" size="large" @click="$router.push('/index-diary')">
    <el-icon><EditPen /></el-icon>
    写日记
  </el-button>
  
  <!-- 新增：财务 -->
  <el-button type="warning" size="large" @click="$router.push('/index-finance')">
    <el-icon><Coin /></el-icon>
    记一笔
  </el-button>
  
  <!-- 新增：照片墙 -->
  <el-button size="large" @click="$router.push('/index-cat')">
    <el-icon><Picture /></el-icon>
    照片墙
  </el-button>
</div>
```

#### 1.2 管理入口下拉菜单

```vue
<!-- components/Navbar.vue - 管理入口修改 -->
<template>
  <div class="navbar">
    <!-- ... 其他内容 ... -->
    
    <div class="right">
      <!-- 管理入口下拉菜单 -->
      <el-dropdown trigger="click">
        <div class="admin-trigger">
          <el-icon><Setting /></el-icon>
          <span class="admin-text">管理</span>
        </div>
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item @click="router.push('/quiz-admin')">
              <el-icon><Collection /></el-icon>
              题库管理
            </el-dropdown-item>
            <el-dropdown-item @click="router.push('/cat')">
              <el-icon><Picture /></el-icon>
              照片管理
            </el-dropdown-item>
            <el-dropdown-item @click="router.push('/finance')">
              <el-icon><Coin /></el-icon>
              财务管理
            </el-dropdown-item>
            <el-dropdown-item divided @click="router.push('/system')">
              <el-icon><Setting /></el-icon>
              系统管理
            </el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>
      
      <!-- 用户下拉菜单 -->
      <el-dropdown>
        <!-- ... 保持不变 ... -->
      </el-dropdown>
    </div>
  </div>
</template>

<style scoped lang="scss">
.admin-trigger {
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 6px 12px;
  border-radius: 6px;
  transition: background 0.2s;
  
  &:hover {
    background: #f5f7fa;
  }
}

.admin-text {
  font-size: 14px;
  color: #606266;
}
</style>
```

#### 1.3 导航命名修改

```vue
<!-- components/Navbar.vue - 导航 Tab 修改 -->
<div class="center" v-if="isUserPage">
  <div class="tab-item" :class="{active: navKey === 'home'}" @click="router.push('/')">
    <el-icon><House /></el-icon>
    首页
  </div>
  <div class="tab-item" :class="{active: navKey === 'diary'}" @click="router.push('/index-diary')">
    <el-icon><Notebook /></el-icon>
    日记
  </div>
  <div class="tab-item" :class="{active: navKey === 'photos'}" @click="router.push('/index-cat')">
    <el-icon><Picture /></el-icon>
    照片墙  <!-- 原"宠物" -->
  </div>
  <div class="tab-item" :class="{active: navKey === 'finance'}" @click="router.push('/index-finance')">
    <el-icon><Coin /></el-icon>
    财务
  </div>
  <div class="tab-item" :class="{active: navKey === 'study'}" @click="router.push('/index-quiz')">
    <el-icon><Reading /></el-icon>
    学习  <!-- 原"做题" -->
  </div>
</div>

<script setup>
import { computed } from 'vue'
import { useRoute } from 'vue-router'

const route = useRoute()

// 判断是否为用户功能页面
const isUserPage = computed(() => {
  const userPages = ['Index', 'IndexDiary', 'IndexCat', 'IndexFinance', 'IndexQuiz', 'IndexQuizWrong', 'IndexQuizFavorites']
  return userPages.includes(route.name)
})

// 当前导航高亮
const navKey = computed(() => {
  const name = route.name
  if (name === 'Index') return 'home'
  if (name === 'IndexDiary') return 'diary'
  if (name === 'IndexCat') return 'photos'
  if (name === 'IndexFinance') return 'finance'
  if (name?.startsWith('IndexQuiz')) return 'study'
  return ''
})
</script>
```

### 4.3 Phase 2: 路由重构（2-3天）

#### 任务清单

| # | 任务 | 文件 | 工作量 |
|---|------|------|--------|
| 2.1 | 创建 Layout 组件 | `layouts/*.vue` | 2h |
| 2.2 | 重构路由配置 | `router/index.js` | 2h |
| 2.3 | 迁移视图文件 | `views/**/*.vue` | 3h |
| 2.4 | 添加旧路由重定向 | `router/index.js` | 0.5h |
| 2.5 | 更新组件内路由引用 | 各组件 | 1h |

### 4.4 Phase 3: 首页重设计（3-4天）

#### 任务清单

| # | 任务 | 文件 | 工作量 |
|---|------|------|--------|
| 3.1 | 创建首页组件结构 | `views/home/` | 1h |
| 3.2 | 实现快速操作卡片组 | `QuickActions.vue` | 2h |
| 3.3 | 实现本周概览组件 | `WeeklyOverview.vue` | 3h |
| 3.4 | 实现最近动态组件 | `RecentActivities.vue` | 4h |
| 3.5 | 创建数据聚合 API | 后端 | 4h |
| 3.6 | 首页样式优化 | CSS | 2h |

#### 3.5 数据聚合 API（后端）

```typescript
// backend/src/dashboard/dashboard.controller.ts

@Controller('dashboard')
export class DashboardController {
  
  // 获取各模块概览数据
  @Get('overview')
  async getOverview(@Query('userId') userId: number) {
    return {
      diary: {
        todayMood: null,  // 今日情绪
        weekCount: 5,     // 本周日记数
        streak: 3         // 连续天数
      },
      photos: {
        total: 56,        // 总照片数
        weekAdded: 3      // 本周新增
      },
      finance: {
        monthExpense: 2345.00,  // 本月支出
        weekRecords: 12,        // 本周记账笔数
        weekExpense: 856.00     // 本周支出
      },
      study: {
        todayCount: 15,         // 今日做题数
        weekCount: 30,          // 本周做题数
        weekAccuracy: '85%'     // 本周正确率
      }
    }
  }
  
  // 获取最近活动
  @Get('activities')
  async getActivities(@Query('userId') userId: number, @Query('limit') limit = 10) {
    // 聚合各模块最近活动，按时间倒序
    return [
      { type: 'diary', time: '2024-12-17 15:30', title: '写了日记「今天很开心」', id: 1 },
      { type: 'finance', time: '2024-12-17 12:20', title: '记账 -88元 [午餐]', id: 101 },
      { type: 'photos', time: '2024-12-17 10:15', title: '上传了3张照片', id: null },
      { type: 'study', time: '2024-12-17 09:00', title: '完成10道题，正确率90%', id: null },
    ]
  }
}
```

---

## 5. 验收标准 (Acceptance Criteria)

### 5.1 Phase 1 验收

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | 首页快速入口 | 首页有日记、财务、照片墙快捷按钮，点击可正常跳转 |
| 2 | 管理入口 | 点击管理按钮出现下拉菜单，包含4个管理模块入口 |
| 3 | 导航命名 | "宠物"已改为"照片墙"，"做题"已改为"学习" |
| 4 | 导航激活 | 各模块页面对应的导航 Tab 正确高亮 |

### 5.2 Phase 2 验收

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | 新路由生效 | `/diary`, `/photos`, `/finance`, `/study` 可正常访问 |
| 2 | 旧路由重定向 | `/index-diary` 等旧路由自动重定向到新路由 |
| 3 | Layout 生效 | 用户功能区使用 AppLayout，管理后台使用 AdminLayout |
| 4 | 管理后台统一 | 所有管理页面路径以 `/admin/` 开头 |

### 5.3 Phase 3 验收

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | 首页布局 | 新首页展示4个模块的快速操作卡片 |
| 2 | 本周概览 | 显示各模块的本周统计数据 |
| 3 | 最近动态 | 显示各模块的最近活动，按时间倒序 |
| 4 | 响应式 | 移动端正常显示，卡片自动换行 |

---

## 6. 风险与应对 (Risks & Mitigation)

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 旧书签失效 | 高 | 中 | 添加旧路由重定向，保留6个月 |
| 用户习惯改变 | 中 | 中 | 发布更新日志说明变化 |
| 后端 API 改动 | 低 | 高 | 首页新 API 独立开发，不影响现有 |
| 移动端适配问题 | 中 | 中 | 各阶段完成后做响应式测试 |

---

## 7. 附录 (Appendices)

### A. 路由对照表

| 旧路由 | 新路由 | 说明 |
|--------|--------|------|
| `/` | `/` | 个人中心（内容重新设计） |
| `/index-diary` | `/diary` | 日记 |
| `/index-cat` | `/photos` | 照片墙 |
| `/index-finance` | `/finance` | 财务 |
| `/index-quiz` | `/study` | 学习 |
| `/index-quiz-wrong` | `/study/wrong` | 错题本 |
| `/index-quiz-favorites` | `/study/favorites` | 我的收藏 |
| `/quiz-admin` | `/admin/quiz` | 题库管理 |
| `/quiz-admin/categories` | `/admin/quiz/categories` | 分类管理 |
| `/quiz-admin/questions` | `/admin/quiz/questions` | 题目管理 |
| `/cat` | `/admin/photos` | 照片管理 |
| `/cat/cat-type` | `/admin/photos/types` | 分类管理 |
| `/cat/cat-info` | `/admin/photos/list` | 照片管理 |
| `/finance/purpose` | `/admin/finance/purpose` | 用途管理 |
| `/finance/budget` | `/admin/finance/budget` | 预算管理 |
| `/finance/record` | `/admin/finance/records` | 记录管理 |
| `/system/user` | `/admin/system/users` | 用户管理 |
| `/system/role` | `/admin/system/roles` | 角色管理 |

### B. 导航配置

```javascript
// src/constants/nav.js

export const APP_NAV_ITEMS = [
  { key: 'home', path: '/', label: '个人中心', icon: 'House' },
  { key: 'diary', path: '/diary', label: '日记', icon: 'Notebook' },
  { key: 'photos', path: '/photos', label: '照片墙', icon: 'Picture' },
  { key: 'finance', path: '/finance', label: '财务', icon: 'Coin' },
  { key: 'study', path: '/study', label: '学习', icon: 'Reading' }
]

export const ADMIN_NAV_ITEMS = [
  { key: 'quiz', path: '/admin/quiz', label: '题库管理', icon: 'Collection' },
  { key: 'photos', path: '/admin/photos', label: '照片管理', icon: 'Picture' },
  { key: 'finance', path: '/admin/finance', label: '财务管理', icon: 'Coin' },
  { key: 'system', path: '/admin/system', label: '系统管理', icon: 'Setting' }
]
```

---

## 8. 下一步行动 (Next Steps)

### 立即行动

| # | 行动 | 负责人 | 截止日期 |
|---|------|--------|---------|
| 1 | 评审本文档，确认优化范围 | PM | 立即 |
| 2 | 执行 Phase 1 快速修复 | Dev | 1-2天 |
| 3 | 测试 Phase 1 改动 | QA | Phase 1完成后 |
| 4 | 执行 Phase 2 路由重构 | Dev | 2-3天 |
| 5 | 执行 Phase 3 首页重设计 | Dev | 3-4天 |

### 开发建议

1. **渐进式实施**：先完成 Phase 1，验证无问题后再进行后续阶段
2. **保持向后兼容**：旧路由重定向至少保留6个月
3. **充分测试**：每个阶段完成后进行完整回归测试
4. **用户沟通**：重大改动前通知用户，发布更新日志

---

*— End of Document —*

*— Mary | Business Analyst —*

